# 13. Development Backlog — Smart Age & BD Date

Checkbox tasks mapped into sprints for full development execution.

---

## 1. Development Phases

### Phase 1: Android Environment Setup
- [ ] Initialize Android Studio project targeting API 35 with Min SDK 26.
- [ ] Implement Hilt, Room, and DataStore dependencies in version catalog.
- [ ] Apply Warm Rose primary palette to `Color.kt` and compile basic Compose container.

### Phase 2: Date Math Engine & Unit Tests
- [ ] Implement `DateMathEngine.kt` using standard java.time libraries.
- [ ] Add unit tests verifying exact age and countdowns.
- [ ] Build test cases targeting leap years (Feb 29) DOB conversions.

### Phase 3: Room Database & Repository
- [ ] Implement `birthday_profiles` and `calculation_history` entity schemas.
- [ ] Build DAO queries returning Flows and verify thread containment.
- [ ] Write `BirthdayRepository` exposing clean database interface to ViewModels.

### Phase 4: Jetpack Compose Frontend
- [ ] Implement bottom-navigation bar with 3 dashboard screens: Age, Diff, and Registry.
- [ ] Style the Result Sheet card overlay with Warm Rose animations.
- [ ] Build the Birthday Registry empty-state screens and Add Profile layout overlays.

### Phase 5: Alarm Notifications & AdMob Release
- [ ] Implement `AlarmScheduler` exact alarm triggers.
- [ ] Add `POST_NOTIFICATIONS` runtime consent popups on registry creation.
- [ ] Configure AdMob SDK Banner, Native list-ads, and 180s Cooldown Interstitial.
- [ ] Initialize Firebase Telemetry and build production release package.

---

## 2. V2 Future Roadmap (Post-Launch)
- [ ] **Home Screen Widget**: Custom calendar grid showing upcoming countdowns directly on the Android home screen.
- [ ] **Celebrity Birthday Database**: Offline database of public figures and historical events to search and compare age gaps.
- [ ] **Aesthetic Customizations**: Allow users to unlock custom accent color schemes (e.g. Lavender, Sky Blue) for their cards.
- [ ] **Local Data Export**: Allow exporting birthdays to an encrypted JSON backup file.
