# 04. Technical Architecture Blueprint — MicroHabit Tracker

All code in this project follows Clean MVVM standards, isolating business logic from the UI framework.

---

## 1. Directory Structure

```
com.saviorsystems.microhabittracker/
├── data/
│   ├── local/
│   │   ├── AppDatabase.kt
│   │   ├── dao/
│   │   │   └── HabitDao.kt
│   │   └── entity/
│   │       ├── HabitEntity.kt
│   │       └── CompletionLogEntity.kt
│   └── repository/
│       └── HabitRepositoryImpl.kt
├── di/
│   ├── DatabaseModule.kt
│   └── RepositoryModule.kt
├── domain/
│   ├── model/
│   │   └── Habit.kt
│   └── repository/
│       └── HabitRepository.kt
├── ui/
│   ├── theme/
│   │   ├── Color.kt
│   │   ├── Theme.kt
│   │   └── Type.kt
│   ├── navigation/
│   ├── screens/
│   │   ├── dashboard/
│   │   │   ├── DashboardScreen.kt
│   │   │   └── DashboardViewModel.kt
│   │   └── settings/
│   └── widget/
│       └── GlanceWidgetReceiver.kt
└── App.kt
```

---

## 2. State Management (ViewModel & StateFlow)

*   ViewModels expose UI state using a read-only Kotlin `StateFlow<DashboardUiState>`.
*   Composables observe the StateFlow using `collectAsStateWithLifecycle()` to prevent resource waste during background states.

```kotlin
sealed interface DashboardUiState {
    data object Loading : DashboardUiState
    data class Success(val habits: List<Habit>) : DashboardUiState
    data object Empty : DashboardUiState
    data class Error(val message: String) : DashboardUiState
}
```

---

## 3. Dependency Injection (Hilt)
*   `DatabaseModule.kt` provides singleton instances of the `AppDatabase` and `HabitDao`.
*   `RepositoryModule.kt` binds `HabitRepositoryImpl` to the repository interface.
