# 13. Backlog Tasks — Prayer Time Helper
`
## Phase 1: Foundation & Location Engine (Week 1)
- [ ] Project scaffolding: Create Android project with Gradle KTS, Version Catalog, and Hilt setup.
- [ ] Implement Material 3 theme with Emerald Green and Midnight Blue palette.
- [ ] Configure `build.gradle.kts` with Room, DataStore, Adhan, Hilt, and Firebase dependencies.
- [ ] Build `UserPreferencesManager` (DataStore) to store location, calculation method, and madhab.
- [ ] Implement `LocationSetupScreen` UI.
- [ ] Implement Foreground Location fetch logic (`FusedLocationProviderClient`) with permissions flow.
- [ ] Implement Manual City Search fallback using Retrofit & Android Geocoder API.
`
## Phase 2: Offline Calculation & Caching (Week 2)
- [ ] Set up Room database (`AppDatabase`) with `PrayerCacheDao`.
- [ ] Implement `AdhanCalculationEngine` wrapping `batoulapps/adhan-kotlin`.
- [ ] Build `PrayerRepository` that checks Room cache -> falls back to `AdhanCalculationEngine`.
- [ ] Implement `HijriDateConverter` to convert Gregorian to Hijri strings natively.
- [ ] Write Unit Tests for `AdhanCalculationEngine` against known Makkah and Dhaka timings.
`
## Phase 3: Main Dashboard UI (Week 2)
- [ ] Build bottom navigation host (Timings, Qibla, Tasbih).
- [ ] Implement `DashboardScreen` displaying the 5 daily prayers and Sunrise.
- [ ] Build `CountdownHeader` component that updates every second using a coroutine `Ticker`.
- [ ] Implement `AlarmConfigBottomSheet` to allow users to toggle Full Azan/Beep/Silent.
`
## Phase 4: Alarm Reliability System (Week 3)
- [ ] Implement `AzanAlarmReceiver` (BroadcastReceiver) to catch alarms.
- [ ] Build `NotificationHelper` to create high-priority notifications with Full Screen Intents.
- [ ] Implement `ExactAlarmScheduler` utilizing `AlarmManager.setExactAndAllowWhileIdle`.
- [ ] Handle Android 12+ `SCHEDULE_EXACT_ALARM` and Android 13+ `POST_NOTIFICATIONS` permissions.
- [ ] Implement `BootReceiver` to recalculate and reschedule alarms when device restarts.
`
## Phase 5: Qibla Compass & Tasbih (Week 4)
- [ ] Implement `QiblaCalculator` to find bearing to Mecca from user's latitude/longitude.
- [ ] Build `CompassViewModel` utilizing `SensorManager` (Accel + Mag) and Low-Pass Filter.
- [ ] Implement `CompassScreen` UI with smooth needle rotation (`animateFloatAsState`).
- [ ] Implement "Figure 8" calibration prompt when sensor accuracy drops.
- [ ] Build `TasbihScreen` with large touch target and haptic feedback on count 33.
`
## Phase 6: Monetization & Settings (Week 5)
- [ ] Integrate Google Mobile Ads SDK with AdMob initialization.
- [ ] Implement Adaptive Banner on Dashboard bottom.
- [ ] Implement Interstitial Ad with strict 4-hour frequency cap on Settings/Tasbih exit.
- [ ] Build `SettingsScreen` with toggles for Calculation Method, Asr Madhab, and Hijri Adjustment.
- [ ] Integrate Firebase Analytics and Crashlytics.
`
## Phase 7: QA & Play Store Release (Week 6)
- [ ] Perform manual testing of alarms in Doze Mode (screen off, disconnected from USB).
- [ ] Test compass rotation across different physical devices.
- [ ] Add Bengali and Hindi localized strings to `strings.xml`.
- [ ] Generate premium App Icon (Mosque/Crescent on Emerald Green).
- [ ] Create 5 framed Play Store screenshots and Feature Graphic.
- [ ] Complete Data Safety Form (declaring no background location tracking).
- [ ] Submit to Google Play Console for Closed Testing (14-day requirement).
`
## Future Enhancements (V2 Backlog)
- [ ] Home Screen Widget showing next prayer time.
- [ ] Lock Screen Notifications for active countdowns.
- [ ] Auto-Silent Mode (mutes phone automatically during congregation time).
- [ ] Monthly Prayer Calendar view.
- [ ] Ramadan specific features (Iftar/Sehri timings dashboard).
`