# 04. Technical Architecture Blueprint — Prayer Time Helper
`
All code in this project follows Clean Architecture with MVVM standards, isolating business logic from the UI framework.
`
---
`
## 1. Directory Structure
`
```
com.saviorsystems.prayertimehelper/
├── data/
│   ├── local/
│   │   ├── AppDatabase.kt                    # Room DB (caching timings)
│   │   ├── dao/
│   │   │   └── PrayerCacheDao.kt             # Read/write daily timings cache
│   │   └── entity/
│   │       └── DailyPrayerEntity.kt          # Cached day record
│   ├── preferences/
│   │   └── UserPreferencesManager.kt         # DataStore (location, calculation method)
│   └── geocoding/
│       └── GeocodingService.kt               # Retrofit API for manual city search
├── domain/
│   ├── engine/
│   │   ├── AdhanCalculationEngine.kt         # Wrapper around batoulapps/adhan-kotlin
│   │   ├── QiblaCalculator.kt                # Spherical trigonometry for Mecca bearing
│   │   └── HijriDateConverter.kt             # Gregorian -> Hijri conversion
│   ├── model/
│   │   ├── DailyTimings.kt                   # Domain model for prayers
│   │   └── LocationParams.kt                 # Lat/Long wrapper
│   └── repository/
│       └── PrayerRepository.kt               # Interface for data fetching
├── ui/
│   ├── theme/
│   │   ├── Color.kt                          # Emerald/Midnight palette
│   │   ├── Theme.kt
│   │   └── Type.kt
│   ├── navigation/
│   │   ├── AppNavHost.kt
│   │   └── BottomNavBar.kt
│   ├── screens/
│   │   ├── dashboard/
│   │   │   ├── DashboardScreen.kt            # Timings list & countdown
│   │   │   └── DashboardViewModel.kt
│   │   ├── qibla/
│   │   │   ├── CompassScreen.kt              # Sensor-driven compass
│   │   │   └── CompassViewModel.kt
│   │   ├── tasbih/
│   │   │   ├── TasbihScreen.kt
│   │   │   └── TasbihViewModel.kt
│   │   ├── location/
│   │   │   ├── LocationSetupScreen.kt        # Permissions gate / City search
│   │   │   └── LocationViewModel.kt
│   │   └── settings/
│   │       ├── SettingsScreen.kt
│   │       └── SettingsViewModel.kt
│   └── components/
│       ├── PrayerTimingRow.kt
│       ├── CountdownHeader.kt
│       └── CompassDial.kt
├── alarm/
│   ├── AzanAlarmReceiver.kt                  # BroadcastReceiver for AlarmManager
│   ├── NotificationHelper.kt                 # Constructs Android Notification
│   └── BootReceiver.kt                       # Reschedules alarms on device reboot
├── di/
│   ├── EngineModule.kt                       # Provides Adhan components
│   ├── DatabaseModule.kt
│   └── RepositoryModule.kt
└── App.kt                                    # @HiltAndroidApp entry point
```
`
---
`
## 2. Technology Stack
`
| Layer | Technology | Version |
| :--- | :--- | :--- |
| **Calculation Engine** | `batoulapps/adhan-kotlin` | 0.0.6+ |
| **UI Framework** | Jetpack Compose (Material 3) | BOM 2024.06+ |
| **Architecture** | MVVM + Clean Architecture | — |
| **DI** | Hilt | 2.51+ |
| **Sensors** | Android SensorManager (Accel/Mag) | API 24+ |
| **Preferences** | DataStore Preferences | 1.1+ |
| **Background Scheduling** | AlarmManager + WorkManager | 2.9+ |
| **Ads** | Google Mobile Ads SDK | 23.0+ |
| **Location** | Play Services Location (Foreground) | 21.2+ |
`
---
`
## 3. The Calculation Engine (`AdhanCalculationEngine`)
`
This is the core of the app. It replaces external APIs.
`
```kotlin
class AdhanCalculationEngine @Inject constructor() {
`
    fun calculateTimings(
        date: LocalDate, 
        latitude: Double, 
        longitude: Double,
        method: CalculationMethod = CalculationMethod.KARACHI,
        madhab: Madhab = Madhab.HANAFI
    ): DailyTimings {
        val coordinates = Coordinates(latitude, longitude)
        val dateComponents = DateComponents(date.year, date.monthValue, date.dayOfMonth)
`
        val parameters = method.parameters.apply {
            this.madhab = madhab
        }
`
        val prayerTimes = PrayerTimes(coordinates, dateComponents, parameters)
`
        return DailyTimings(
            fajr = prayerTimes.fajr,
            sunrise = prayerTimes.sunrise,
            dhuhr = prayerTimes.dhuhr,
            asr = prayerTimes.asr,
            maghrib = prayerTimes.maghrib,
            isha = prayerTimes.isha
        )
    }
}
```
`
---
`
## 4. Alarm System Architecture (Critical for Reliability)
`
Android's battery optimizations (Doze mode) routinely kill background apps. To ensure Azan alarms fire exactly on time:
`
1.  **Scheduling**: Use `AlarmManager.setExactAndAllowWhileIdle()` (API 23+) or `setAlarmClock()` for precise triggering.
2.  **Receiver**: `AzanAlarmReceiver` catches the intent and immediately triggers a foreground notification with a full-screen intent (optional) and audio payload.
3.  **Boot Persistence**: Android clears AlarmManager on device reboot. `BootReceiver` listens for `ACTION_BOOT_COMPLETED` and immediately invokes the WorkManager to recalculate and re-register alarms for the upcoming days.
`
---
`
## 5. Compass Sensor Fusion
`
A raw magnetometer is too jittery for a smooth UI.
*   We use a `SensorManager.getRotationMatrix()` combining `TYPE_ACCELEROMETER` and `TYPE_MAGNETIC_FIELD`.
*   We apply a Low-Pass Filter (LPF) algorithm to the raw sensor data in `CompassViewModel` to smooth the needle rotation before emitting it to the Compose UI.
`